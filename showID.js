const showId = (name) => {
  if (name === "Francis") {
    return `${name} is a name`;
  } else if (name === "Jean-Eluard") {
    return `${name} is a name`;
  } else if (name === "Bernard-Henry-Metal") {
    return `${name} is a name`;
  } else {
    return `${name} is not a name`;
  }
};

showId("Francis");

// переделать так чтоб условия совпадали имена могут быть только с заглавной буквы + не может быть пробела
